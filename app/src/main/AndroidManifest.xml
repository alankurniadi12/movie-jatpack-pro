<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alankurniadi.submission2jatpackpromovie">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.bookmark.BookmarkActivity"></activity>
        <activity android:name=".ui.bookmark.tv.BookmarkTvActivity" />
        <activity android:name=".ui.bookmark.movie.BookmarkMovieActivity" />
        <activity android:name=".ui.detail.tv.DetailTvActivity" />
        <activity android:name=".ui.detail.movie.DetailMovieActivity" />
        <activity android:name=".ui.home.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>